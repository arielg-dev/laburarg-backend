services:
  # ---- Base de datos MySQL ----
  db:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3307:3306"
    # healthcheck: un comando que Docker ejecuta periódicamente
    # DENTRO del contenedor, para determinar si el servicio ya
    # está realmente operativo (no solo "el proceso existe").
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ---- Redis (cache y cola de mensajes para Celery) ----
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"

  # ---- Nuestro backend Django ----
  web:
    build: .
    restart: unless-stopped
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=db
    env_file:
      - .env
    depends_on:
      # Esperamos a que el healthcheck de "db" confirme "healthy"
      # (MySQL realmente aceptando conexiones), no solo a que el
      # contenedor exista.
      db:
        condition: service_healthy
      redis:
        condition: service_started

volumes:
  mysql_data: